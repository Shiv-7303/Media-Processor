services:
  - type: web
    name: media-processor
    runtime: python
    buildCommand: "bash build.sh"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0